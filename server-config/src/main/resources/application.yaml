server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: testing #Rama de git
          uri: https://github.com/MaximilianoLutz/invitacion
          search-paths: /config-data
  application:
    name: microservicio-config