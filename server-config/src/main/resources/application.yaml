server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: multiple-instances #Rama de git
          uri: https://github.com/MaximilianoLutz/invitacion
          search-paths: /config-data
  application:
    name: microservicio-config